%p
  Do a csv export from the
  #{link_to "esuite software", "http://www.esuite.com.au/"} and then upload the file here

= form_tag({action: "create"}, multipart: true) do
  .form-group
    = file_field_tag 'export'
  = submit_tag("Upload", class: "btn btn-default")
